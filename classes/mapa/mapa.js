/* function hasOwnProperty(valor){
    if(valor)console.log(`true`);
    else{
        console.log(`false`);
    }
} */
let mapa = {uno: true, doe: true};

console.log(mapa.hasOwnProperty("uno"));
/* hasOwnProperty.call(mapa); */